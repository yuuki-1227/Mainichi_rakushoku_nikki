<table>
  <% posts.each do |post| %>
  <tr>
    <td>
      <%= link_to post_path(post) do %>
        <%= attachment_image_tag post, :image, :fill, 100, 100, fallback: "no_image.jpg" %>
      <% end %>
    </td>
    <td><%= post.title %></td>
    <td><%= post.explain %></td>
  </tr>
  <% end %>
</table>